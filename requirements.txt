# =============================================================================
# AI Code Review v2.0 - Dependências Python
# Instalar com: pip install -r requirements.txt
# =============================================================================

# --- Obrigatórias ---
requests>=2.28.0          # Chamadas HTTP para APIs de LLM e Azure DevOps
PyYAML>=6.0               # Leitura de ficheiros de configuração YAML
boto3>=1.34.0             # Integração com AWS Bedrock

# --- Providers LLM (instalar conforme necessidade) ---
# Nota: Todos os providers funcionam via requests (HTTP directo).
# Os SDKs oficiais abaixo são OPCIONAIS e oferecem funcionalidades extras.

# Google Gemini (opcional - funciona via requests sem SDK)
# google-generativeai>=0.3.0

# Anthropic Claude (opcional - funciona via requests sem SDK)
# anthropic>=0.18.0

# OpenAI (opcional - funciona via requests sem SDK)
# openai>=1.0.0

# --- Opcionais (melhorias de UX) ---
# rich>=13.0.0             # Formatação avançada de terminal
# colorama>=0.4.6          # Cores no terminal Windows

# --- Tree-sitter (opcional — extração de contexto AST) ---
# Usado em src/context_extractor.py para Enclosing Scopes e File Skeletons.
# Se não instalado, o módulo faz fallback automático para Adaptive Diff.
# tree-sitter-languages>=1.10.2  # Parsers pré-compilados para C#, Java, JS, TS, Python, SQL
